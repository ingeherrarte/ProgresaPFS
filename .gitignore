# Código legacy y archivos que no se suben a GitHub
/www-old/
/www-old.zip
/qa_guatecompras/
/buscarecibopfs.php

# Variables de entorno con credenciales
www/.env
/.env

# Datos y dependencias
mysql-data/
volumes/
vendor/
*.log

# Backups de BD (contienen datos reales, ej. hashes de contraseñas)
*_backup.sql
/backups/
/backups-qa/

# Config de rclone (incluye el token OAuth de Dropbox)
/rclone/

# Fotos de boletas de depósito subidas por los usuarios
www/uploads/

# Config local de Claude Code
www/.claude/
