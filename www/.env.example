# Copiar este archivo a .env y reemplazar los valores con los reales del servidor.
# El archivo .env real nunca se sube a git (ver .gitignore).
DB_HOST=localhost
DB_NAME=cetecpro
DB_USER=cambia_este_usuario
DB_PASS=cambia_esta_contrasena
